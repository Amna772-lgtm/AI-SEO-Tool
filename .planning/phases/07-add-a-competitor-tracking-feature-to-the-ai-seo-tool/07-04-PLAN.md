---
phase: 07-add-a-competitor-tracking-feature-to-the-ai-seo-tool
plan: 04
type: execute
wave: 4
depends_on: [07-03]
files_modified:
  - frontend/app/page.tsx
autonomous: false
requirements: [D-01, D-02, D-14, D-19]
must_haves:
  truths:
    - "The Competitors tab appears in the main navigation for ALL authenticated users (Free, Pro, Agency) — always visible per D-01"
    - "Clicking the Competitors tab as a Free user shows the LockedFeature upgrade prompt (rendered inside CompetitorsTab)"
    - "Clicking the Competitors tab as a Pro/Agency user shows the primary-site selector + Find Competitors CTA"
    - "The full end-to-end flow works: select primary → Find Competitors → check suggestions → Add Competitors → competitor cards appear with Auditing... → cards transition to complete → comparison view (SiteComparisonCard + CompetitorRadarChart) appears"
    - "Adding a 4th competitor as a Pro user surfaces the cap-reached error copy in the UI"
  artifacts:
    - path: "frontend/app/page.tsx"
      provides: "Competitors tab wired into MainTab union, nav array, and conditional tab panel"
      contains: "mainTab === \"competitors\""
  key_links:
    - from: "MainTab union type"
      to: "\"competitors\" string literal"
      via: "type extension"
      pattern: "\"competitors\""
    - from: "nav array"
      to: "new { id: 'competitors', ... } entry"
      via: "inline JSX array literal"
      pattern: "id: \"competitors\""
    - from: "tab panel conditional"
      to: "<CompetitorsTab />"
      via: "{mainTab === \"competitors\" && (...)}"
      pattern: "mainTab === \"competitors\""
---

<objective>
Wire the CompetitorsTab component from Plan 03 into the main page shell. This is a small, surgical edit to frontend/app/page.tsx: extend the MainTab union, add a nav entry, and add a conditional tab panel. Then a human UAT checkpoint confirms the end-to-end flow works against a running backend (Plan 02) with real Claude API calls.

Purpose: Plan 03 built the tab in isolation. This plan makes it reachable in the UI and validates the complete phase against live infrastructure.
Output: One file modified (page.tsx), one human verification checkpoint.
</objective>

<execution_context>
@e:/AI SEO Tool/.claude/get-shit-done/workflows/execute-plan.md
@e:/AI SEO Tool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-add-a-competitor-tracking-feature-to-the-ai-seo-tool/07-CONTEXT.md
@.planning/phases/07-add-a-competitor-tracking-feature-to-the-ai-seo-tool/07-UI-SPEC.md
@.planning/phases/07-add-a-competitor-tracking-feature-to-the-ai-seo-tool/07-01-PLAN.md
@.planning/phases/07-add-a-competitor-tracking-feature-to-the-ai-seo-tool/07-02-PLAN.md
@.planning/phases/07-add-a-competitor-tracking-feature-to-the-ai-seo-tool/07-03-PLAN.md

@frontend/app/page.tsx
@frontend/app/components/competitors/CompetitorsTab.tsx

<interfaces>
<!-- Exact existing lines in page.tsx that must be modified. -->

Line 30 (current):
```typescript
type MainTab = "dashboard" | "crawl" | "audit" | "geo" | "insights" | "history" | "schedules";
```
Must become:
```typescript
type MainTab = "dashboard" | "crawl" | "audit" | "geo" | "insights" | "history" | "schedules" | "competitors";
```

Lines ~640-648 (nav array): A new entry for `competitors` must be added AFTER `schedules`:
```typescript
{ id: "schedules", label: "Schedules", icon: <svg.../> },
{ id: "competitors", label: "Competitors", icon: <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><circle cx="9" cy="9" r="4"/><circle cx="17" cy="17" r="4"/><line x1="12.5" y1="12.5" x2="13.5" y2="13.5"/></svg> },
```

Lines ~1420-1440 area (tab panels): After the existing `{mainTab === "schedules" && ...}` block, add:
```typescript
{mainTab === "competitors" && (
  <div className="space-y-6 p-8">
    <CompetitorsTab />
  </div>
)}
```
Note: no `quotaExhausted` check — CompetitorsTab handles its own plan gating (D-19: LockedFeature renders INSIDE the tab component, not conditional here). Tab is visible even when quota is exhausted because the user might still want to view existing competitor comparisons.

Import at top of file (near other component imports around line 27):
```typescript
import CompetitorsTab from "./components/competitors/CompetitorsTab";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire CompetitorsTab into page.tsx (MainTab union + nav + tab panel + import)</name>
  <files>frontend/app/page.tsx</files>
  <read_first>
    - frontend/app/page.tsx (ENTIRE FILE — identify exact line numbers for MainTab type, nav array, each tab panel conditional, the imports block)
    - frontend/app/components/competitors/CompetitorsTab.tsx (verify the default export name and that it takes no props)
    - .planning/phases/07-add-a-competitor-tracking-feature-to-the-ai-seo-tool/07-UI-SPEC.md (Layout Blueprint — Competitors tab positioned last in the nav order)
  </read_first>
  <action>
    Make four surgical edits to `frontend/app/page.tsx`:

    1. **Add import** (near the top of the file, alongside other component imports like `import LockedFeature from "./components/LockedFeature";` on line 27). Insert after the last component import:
       ```typescript
       import CompetitorsTab from "./components/competitors/CompetitorsTab";
       ```

    2. **Extend MainTab union** (line 30). Change from:
       ```typescript
       type MainTab = "dashboard" | "crawl" | "audit" | "geo" | "insights" | "history" | "schedules";
       ```
       to:
       ```typescript
       type MainTab = "dashboard" | "crawl" | "audit" | "geo" | "insights" | "history" | "schedules" | "competitors";
       ```

    3. **Add nav entry** (in the nav array around line 648, after the schedules entry and before the closing `] as { id: MainTab; ... }[]`). Insert:
       ```typescript
       { id: "competitors", label: "Competitors", icon: <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><circle cx="9" cy="9" r="4"/><circle cx="17" cy="17" r="4"/><line x1="12.5" y1="12.5" x2="13.5" y2="13.5"/></svg> },
       ```
       The SVG draws two overlapping circles, representing competitor comparison. Keep the same 14×14 dimensions as other nav icons for visual consistency.

    4. **Add tab panel** (after the existing `{mainTab === "schedules" && ...}` block around line 1426-1435). Insert:
       ```tsx
       {mainTab === "competitors" && (
         <div className="space-y-6 p-8">
           <CompetitorsTab />
         </div>
       )}
       ```
       IMPORTANT: Do NOT guard with `!quotaExhausted` — the competitors tab handles its own plan gating via LockedFeature inside CompetitorsTab, and users should still reach the tab even if they're over quota (to view existing comparisons). This deliberately differs from the dashboard/crawl/insights panels.

    Verify the diff is exactly 4 inserts — no deletions, no modifications to existing tab handlers, no changes to the sidebar layout, no changes to authLoading logic. The goal is a minimal surgical patch.

    Run `cd frontend && npx tsc --noEmit` after the edit. Then run `cd frontend && npm run lint` if a lint script exists in package.json.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&amp;1 | grep -E "page.tsx|CompetitorsTab" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import CompetitorsTab from \"./components/competitors/CompetitorsTab\"" frontend/app/page.tsx` matches
    - `grep "\"schedules\" | \"competitors\"" frontend/app/page.tsx` matches
    - `grep "id: \"competitors\", label: \"Competitors\"" frontend/app/page.tsx` matches
    - `grep "mainTab === \"competitors\"" frontend/app/page.tsx` matches
    - `grep -c "<CompetitorsTab />" frontend/app/page.tsx` returns 1
    - `cd frontend && npx tsc --noEmit` exits 0
    - No other tab panel handlers modified (`git diff frontend/app/page.tsx` shows only additions, no deletions of existing mainTab blocks)
  </acceptance_criteria>
  <done>page.tsx has CompetitorsTab imported, wired into MainTab union, rendered in nav, and mounted as a conditional tab panel. TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — end-to-end Competitor Tracking flow</name>
  <files>(manual verification — no files modified)</files>
  <action>Human-only UAT — execute the test scenarios in <how-to-verify> against the running backend + frontend. Do not modify any files in this task.</action>
  <verify>Manual browser verification per how-to-verify block below.</verify>
  <done>All 7 UAT scenarios pass and regression check is clean; user types "approved".</done>
  <what-built>
    The entire Competitor Tracking feature end-to-end:
    - Backend: competitor_groups + competitor_sites tables, /competitors/ API router with 7 routes, Claude-powered discovery
    - Frontend: Competitors tab in main nav, primary-site selector, discovery flow with suggestion cards, manual URL input, competitor cards with polling, side-by-side comparison, custom SVG radar chart
    - Plan gating: Free users see LockedFeature; Pro=3 cap; Agency=10 cap
    - Quota integration: competitor audits burn the monthly quota; re-audit burns another slot
  </what-built>
  <how-to-verify>
    Prerequisites: Backend running via `docker compose up`, frontend running via `cd frontend && npm run dev`, ANTHROPIC_API_KEY set in backend .env, at least one completed audit in history for the test user.

    **Test 1 — Tab Visibility for Free User**
    1. Sign up a new user (no subscription = free plan by default), or sign in as an existing free user
    2. Observe the left sidebar nav — the "Competitors" tab should be visible (NOT hidden)
    3. Click the Competitors tab
    4. Expected: LockedFeature prompt appears with "Competitor Tracking" title, "Available on Pro plan" sub-copy, and an upgrade CTA button
    5. Expected: NO selector, NO suggestion area, NO competitor cards visible

    **Test 2 — Pro User Full Flow**
    1. Sign in as a Pro user who has at least 2 completed audits in History (e.g., example.com, test.com)
    2. Click the Competitors tab
    3. Verify: dropdown labeled "Your site" is populated with the distinct domains (deduped)
    4. Verify: "Find Competitors" button is DISABLED until a site is chosen
    5. Select one of the audited sites from the dropdown
    6. Verify: "Find Competitors" button becomes enabled (accent color)
    7. Click "Find Competitors"
    8. Verify: Button changes to spinner + "Finding competitors..." label
    9. Wait ≤15 seconds for Claude response
    10. Expected: 5-8 suggestion cards appear with "Found N competitors — select to audit" heading, each card showing a domain in monospace + one-line reason + checkbox
    11. Check 2 of the suggestion cards
    12. Verify: "Add 2 Competitors" button becomes active
    13. Click "Add 2 Competitors"
    14. Expected: Two CompetitorCard elements appear in the competitor row, each showing a spinner + "Auditing..." label
    15. Wait ≤2 minutes (typical audit duration)
    16. Expected: Cards transition one by one to the complete state — GeoScoreRing with score + grade + domain + site type badge + "Re-audit" and "Remove" buttons
    17. Expected: Once at least one competitor is complete, the comparison view appears below:
        - Left panel: "Score Comparison" section with side-by-side cards (primary + each complete competitor), each showing GeoScoreRing + EngineScores
        - Right panel: "GEO Dimension Comparison" with the CompetitorRadarChart — a hexagonal SVG with 6 labeled axes (NLP, Schema, E-E-A-T, Content, Entity, Technical), one colored polygon per site, legend below
    18. Verify the radar chart Entity axis shows a non-zero value when the primary site has entity data (this is the Pitfall 1 check)

    **Test 3 — Pro Competitor Cap**
    1. Continue as the Pro user. With 2 competitors already added, add 1 more via the manual "Add a competitor URL manually" input — e.g., "https://somecompetitor.com" → click "Add URL"
    2. Expected: third card appears in Auditing state
    3. Try to add a 4th competitor via manual input
    4. Expected: An error surfaces in the UI with copy similar to "Pro plan supports up to 3 competitors per group."
    5. No 4th card appears

    **Test 4 — Manual Add + Discovery Fallback**
    1. With a primary site selected, use the manual URL input to add a competitor without clicking Find Competitors first
    2. Expected: works normally
    3. To test Claude fallback: temporarily set ANTHROPIC_API_KEY="" in backend .env, restart backend, click Find Competitors
    4. Expected: fallback message appears: "Couldn't find suggestions right now. Add competitors manually using the field below."
    5. Restore ANTHROPIC_API_KEY and restart backend

    **Test 5 — Re-audit**
    1. On a completed competitor card, click "Re-audit"
    2. Expected: card transitions back to pending (spinner + Auditing...) and then re-completes
    3. Verify in the Subscription/History view that monthly audit usage incremented

    **Test 6 — Remove Competitor**
    1. Click "Remove" on a competitor card
    2. Expected: inline confirmation appears within the card ("Remove {domain}? This cannot be undone.") with "Keep Competitor" and "Remove" buttons
    3. Click "Remove"
    4. Expected: card disappears; radar chart polygon count decreases by 1

    **Test 7 — Agency Plan Cap**
    1. Sign in as an Agency user
    2. Add 10 competitors (use manual URL input with distinct URLs)
    3. Expected: all 10 succeed
    4. Attempt to add an 11th
    5. Expected: "Agency plan supports up to 10 competitors per group." error

    **Regression check:**
    1. Navigate to Dashboard, Crawl, Technical, GEO, Insights, History, Schedules tabs
    2. Verify each still functions as before (crawl progress, history comparison, schedule creation — all untouched)
    3. Verify the sidebar Sign Out button still works
    4. Verify no console errors in browser devtools related to the new tab
  </how-to-verify>
  <resume-signal>
    Type "approved" once all 7 tests pass and the regression check is clean.
    If any test fails, describe the failure (which test, what you observed vs expected, any console errors) and Claude will produce a gap-closure plan.
  </resume-signal>
</task>

</tasks>

<verification>
- Manual UAT across 7 test scenarios (see checkpoint above)
- `cd frontend && npx tsc --noEmit` → 0 errors
- `docker exec -it ai_seo_backend pytest backend/tests/ -q` → all green (no regressions)
- Full phase 07 requirements D-01..D-19 satisfied
</verification>

<success_criteria>
1. Competitors tab visible in nav for all authenticated users
2. Free user sees LockedFeature on tab click
3. Pro user can discover, add, view comparison for up to 3 competitors; 4th is rejected with cap_reached copy
4. Agency user can add up to 10 competitors
5. Re-audit burns a quota slot
6. Radar chart shows all 6 axes with entity sourced from geo_data.entity.entity_score
7. Polling terminates when all sites are complete (no runaway setInterval)
8. Claude failure shows fallback copy with manual input still available
9. No regressions in pre-existing tabs (Dashboard, History, Schedules, etc.)
10. UAT checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-a-competitor-tracking-feature-to-the-ai-seo-tool/07-04-SUMMARY.md` documenting: the 4 surgical edits to page.tsx, the UAT test results, any gaps found and logged for gap-closure, and a final requirement coverage matrix (D-01 through D-19 → plan/task that implements it).
</output>
