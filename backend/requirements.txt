# API
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0

# Worker (celery must be installed for "celery" executable in container)
celery[redis]>=5.3.0

# Crawl store + broker
redis>=5.0.0

# Crawler
httpx>=0.24.0
beautifulsoup4>=4.12.0

# GEO agents
anthropic>=0.30.0
textstat>=0.7.0
reportlab>=4.0.0
lxml>=4.9.0

# Auth
PyJWT>=2.12.1
bcrypt>=5.0.0

# Tests
pytest>=7.0.0
pytest-asyncio>=0.21.0
